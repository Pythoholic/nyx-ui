# @nyx-ui/core

The CSS foundation for Nyx UI. It provides design tokens, themes, base styles, motion rules, and component styles without a JavaScript runtime.

```css
@import "@nyx-ui/core";
```

Nyx UI uses Tailwind CSS 4, but the published stylesheet can also be consumed directly. See the repository documentation for theme and component markup contracts.
